data_dir: data
download: true

hidden_dim: 64
num_heads: 8
num_blocks: 8
patch_size: 2
window_threshold: 256.0

batch_size: 256
eval_batch_size: 256
epochs: 20
max_steps: null
max_train_batches: null
max_val_batches: null
train_subset: null
val_subset: null

lr: 0.01
betas:
  - 0.9
  - 0.95
weight_decay: 0.0001

log_every: 50
seed: 123
device: auto
num_workers: 2
gradient_checkpointing: true
precision: fp32

wandb_project: screening-mnist
wandb_run_name: null
wandb_mode: online # options: online, offline, disabled
checkpoint_path: output/mnist-05.ckpt
